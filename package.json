{
  "name": "helpdesk",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "pnpm": {
    "overrides": {
      "@next/swc": "15.5.7",
      "@next/swc-win32-x64-msvc": "15.5.7"
    }
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:all": "docker compose up -d && pnpm --filter @helpdesk/api prisma:migrate && pnpm --filter @helpdesk/api exec tsx src/scripts/seed.ts && pnpm dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
